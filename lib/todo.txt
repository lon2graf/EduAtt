ЧТО ПЛАНИРУЮ РЕАЛИЗОВАТЬ В ПРОГРАММЕ

1. ОФФЛАЙН РЕЖИМ
    В ЛОКАЛЬНОЙ БД(HIVE DART), при запуске проверяется наличие интернет соединения, если он есть то грузим данные из бд, если нет,
    то грузим данные из локального хранилища

2. МАСКОТ
    Сделать забавного маскота(либо котик, либо ленивец), который будет разбавлять серьезность приложения и добавить ироничноть и забавность

3. Работа с аватарками (изи)

4. ВОЗМОЖНОСТЬ ПРИКРЕПЛЕНИЯ СПРАВОК, ОБЪЯСНИТЕЛЬНЫХ К ПРОПУСКАМ
    МБ сделать конструктор объяснительных хз хз хз 

5. ОТМЕТКА ПО QR коду 

6. ПОДТВЕРЖДЕНИЕ ПОСЕЩАЕМОСТИ ПРЕПОДАВАТЕЛЕМ ПЕРЕД ОТПРАВКОЙ ДАННЫХ НА БАЗУ
+ ВОЗМОЖНОСТЬ ПЕРЕХВАТА УПРАВЛЕНИЯ ПРЕПОДАВАТЕЛЕМ
    Статусы урока (LessonStatus):

    🟢 Free — Урок свободен, редактировать могут оба.

    🟡 OnHeadmanEditing — Зашел староста. Преподаватель видит статус, но (опционально) может перехватить.

    🟠 WaitConfirmation — Староста сохранил. Данные отправлены на проверку.

    🔴 OnTeacherEditing — Зашел преподаватель. Староста полностью заблокирован (Read-only).

    🔒 Confirmed — Финал. Данные утверждены, редактирование закрыто.

Основные сценарии (User Flows)

6.1. Сценарий «Преподаватель делает всё сам»

    Урок Free.

    Преподаватель заходит ➔ Статус OnTeacherEditing.

    Староста видит: "Заполняет преподаватель" (доступ закрыт).

    Преподаватель отмечает и жмет "Подтвердить" ➔ Статус Confirmed.

6.2. Сценарий «Работа Старосты» (Основной)

    Урок Free.

    Староста заходит ➔ Статус OnHeadmanEditing.

    Преподаватель видит: "Заполняет староста".

    Староста жмет "Сохранить" ➔ Статус WaitConfirmation.

    Преподаватель видит заявку, заходит проверить ➔ Статус OnTeacherEditing (перехват для проверки).

    Преподаватель жмет "Подтвердить" ➔ Статус Confirmed.

6.3. Сценарий «Отмена редактирования» (Важно!)

    Староста зашел случайно (Статус стал OnHeadmanEditing).

    Нажал кнопку «Назад» (не сохраняя).

    Система обязана вернуть статус в Free, чтобы урок не остался заблокированным навечно.

6.4. Сценарий «Перехват» (Force Unlock)

    Староста зашел (OnHeadmanEditing), но забыл выйти или у него вылетел интернет.

    Преподаватель не ждет, заходит сам.

    Система принудительно меняет статус на OnTeacherEditing. Староста теряет доступ при следующей попытке сохранить.


6.5. Механизм защиты от конфликтов (Pessimistic Locking)

Используем паттерн Check-then-Act (Проверь-потом-Действуй) вместо сложных сокетов.

    На входе (Нажатие «Отметить»):

        Приложение делает легкий запрос к БД (getFreshStatus).

        Если статус изменился (кто-то занял урок раньше) ➔ Блокируем вход, обновляем UI.

        Если статус ОК ➔ Меняем статус в БД на On...Editing, обновляем локальный провайдер, пускаем на экран.

    На выходе (Нажатие «Сохранить»):

        Снова проверяем статус в БД.

        Если статус изменился (Преподаватель перехватил) ➔ Блокируем сохранение, показываем уведомление, выкидываем на главную.

6.6. Логика перехвата (Teacher Override)

Если статус OnHeadmanEditing, преподаватель может принудительно забрать управление.

    Действие: Преподаватель нажимает «Перехватить».

    БД: Статус меняется на OnTeacherEditing.

    Данные: Преподавателю открывается пустой список (данные старосты игнорируются/сбрасываются).

    Староста: При попытке сохранить получает ошибку и теряет свои несохраненные данные.

6.7. Стратегия загрузки данных (Data Fetching)

Чтобы не грузить лишнее из БД:

    Если статус Free или On...Editing (Начало работы) ➔ Инициализируем чистый список из группы студентов (без запроса отметок).

    Если статус WaitConfirmation или Confirmed (Проверка) ➔ Загружаем существующие отметки из БД и накладываем на список студентов.

кароч перед нажатием кнопки отметить посещаемость надо проверять статус урока
для реализации перехвата, также перед нажатием сохранить посещаемость у старосты, нужно проверять статус урока


7. Темная и светлая темы